:root {
    --bg-color: #121212;
    --primary-color: #00bcd4; /* Cian Tecnológico */
    --secondary-color: #ff4081; /* Rosa Neón */
    --text-color: #e0e0e0;
    --panel-bg: #1e1e1e;
    --halo: #00ffff; /* Resplandor */
    --error-color: #ef5350;
    --success-color: #66bb6a;
}

/* Estilos Generales y Tipografía */
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: var(--bg-color);
    color: var(--text-color);
    margin: 0;
    padding: 0;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
}

header {
    background: var(--panel-bg);
    padding: 20px 40px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 2px solid var(--primary-color);
}

header h1 {
    font-size: 1.8em;
    margin: 0;
    color: var(--primary-color);
    text-shadow: 0 0 5px var(--halo);
}

.subtitle {
    font-size: 0.6em;
    opacity: 0.7;
    margin-left: 10px;
    color: var(--text-color);
}

/* Contenedores de Layout */
.container {
    display: flex;
    flex-wrap: wrap;
    gap: 30px;
    padding: 30px;
    max-width: 1400px;
    margin: 0 auto;
    width: 100%;
    flex-grow: 1;
}

.avatar-interface {
    flex: 2;
    min-width: 350px;
    display: flex;
    flex-direction: column;
    gap: 20px;
}

.panels-wrap {
    flex: 1;
    min-width: 300px;
    display: flex;
    flex-direction: column;
    gap: 20px;
}

/* Paneles y Secciones */
.panel {
    background-color: var(--panel-bg);
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
    border-left: 3px solid var(--secondary-color);
}

.panel h2, .status-panel h2 {
    color: var(--secondary-color);
    border-bottom: 1px solid rgba(255, 64, 129, 0.3);
    padding-bottom: 10px;
    margin-top: 0;
    font-size: 1.4em;
}

/* Botones y Formulario */
button, .utility-buttons a button {
    background-color: var(--primary-color);
    color: var(--bg-color);
    border: none;
    padding: 10px 15px;
    border-radius: 5px;
    cursor: pointer;
    font-weight: bold;
    transition: background-color 0.2s, box-shadow 0.2s;
}

button:hover:not(:disabled) {
    background-color: #00a0ae;
    box-shadow: 0 0 8px var(--primary-color);
}

button:disabled {
    background-color: #333;
    color: #888;
    cursor: not-allowed;
}

.secondary {
    background-color: var(--secondary-color);
}

.secondary:hover:not(:disabled) {
    background-color: #e53775;
    box-shadow: 0 0 8px var(--secondary-color);
}

.utility-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 20px;
}

/* Input y Formulario de Interacción */
.interaction-form {
    display: flex;
    gap: 10px;
    margin-top: 15px;
}

#essenceInput {
    flex-grow: 1;
    padding: 10px;
    border-radius: 5px;
    border: 1px solid var(--secondary-color);
    background-color: #2a2a2a;
    color: var(--text-color);
}

/* Avatar Visuals */
.canvas-wrap {
    position: relative;
    width: 100%;
    /* Altura basada en el ratio de una imagen de 400px */
    height: 400px; 
    border: 1px solid var(--halo);
    border-radius: 8px;
    overflow: hidden;
    background-color: #000;
}

.avatar-state {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0;
    transition: opacity 0.2s ease-in-out;
}

.avatar-state.active {
    opacity: 1;
}

/* Vitality Bar */
.vitality-bar {
    position: absolute;
    bottom: 0;
    left: 0;
    height: 15px;
    background: linear-gradient(90deg, var(--secondary-color) 0%, var(--primary-color) 100%);
    width: 100%; /* El JS ajusta esto */
    transition: width 0.5s ease-out;
    opacity: 0.8;
}

/* Log de Consola */
.log-container {
    background-color: #0d0d0d;
    border: 1px solid #333;
    padding: 10px;
    height: 200px;
    overflow-y: auto;
    border-radius: 5px;
    font-size: 0.9em;
    color: #999;
    box-shadow: inset 0 0 5px rgba(0, 0, 0, 0.7);
}

.log-container p {
    margin: 3px 0;
    line-height: 1.3;
}

/* Separador en Pagos */
.separator {
    border: 0;
    height: 1px;
    background: linear-gradient(to right, transparent, var(--secondary-color), transparent);
    margin: 20px 0;
}

/* Información de Wallet */
.wallet-info p {
    margin: 8px 0;
}

.payment-form h3 {
    color: var(--primary-color);
    margin-top: 0;
    font-size: 1.1em;
}

.payment-form input {
    width: calc(100% - 22px);
    padding: 10px;
    margin-bottom: 10px;
    border-radius: 5px;
    border: 1px solid #444;
    background-color: #2a2a2a;
    color: var(--text-color);
}
